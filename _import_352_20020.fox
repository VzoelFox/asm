fungsi json_get(val, key)
  tipe val JsonValue
  var type = val.type
  var res = 0 ; Null ptr

  jika (type == 3) ; Object
     var map = val.obj_val
     res = map_get(map, key)
  tutup_jika

  asm_mulai
  mov rax, [var_res]
  tutup_asm
tutup_fungsi
